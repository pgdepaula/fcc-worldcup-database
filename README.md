# World Cup Database

Relational database project from the freeCodeCamp curriculum.

The project creates a PostgreSQL database for World Cup matches, imports data from `games.csv`, and runs SQL queries to produce tournament statistics.
